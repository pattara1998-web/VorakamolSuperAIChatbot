# รายงานตรวจสอบ + แบบออกแบบระบบ: "AI เป็นสมองหนึ่งเดียว / ไม่มีราคาเริ่มต้น"

> เอกสารนี้เขียนจากการอ่านโค้ดจริงในเวิร์กสเปซนี้ (ไม่ใช่จากการเชื่อเอกสาร handoff)
> ตรวจเมื่อ: 2026-09-16 | โหมด: Architect | ยังไม่แก้ไขโค้ดใด ๆ ทั้งสิ้น

---

## 1. สรุปความเข้าใจความต้องการของผู้ใช้

1. **"เอาราคาเริ่มต้นออกไปจากระบบเลย"** — ห้ามมีแนวคิด/คำว่า "ราคาเริ่มต้น" หลุดไปถึงลูกค้าอีก
   ทั้งจากโค้ด, จาก prompt, จาก UI และจากข้อมูลเก่าในฐานข้อมูล (ต้นตอจริงของ ฿990 ในภาพหน้าจอ)
2. **"เปลี่ยนระบบการตอบเป็น AI เป็นสมอง"** — AI รับข้อความลูกค้า → ดึงข้อมูลสินค้า/โปร/ค่าส่ง
   ที่เจ้าของร้านกรอกไว้มาเรียบเรียงตอบเองแบบมนุษย์แอดมิน ไม่ใช่บอทยิงสคริปต์สเต็ป
3. ให้ตรวจสอบข้อความ handoff ที่ AI ตัวก่อนสรุปมา ว่าตรงกับโค้ดจริงหรือไม่ ก่อนเชื่อและทำงานต่อ

---

## 2. ผลตรวจสอบ handoff เทียบโค้ดจริง

### 2.1 ข้อความที่ "ตรงจริง" ✅

| คำอ้างใน handoff | หลักฐานในโค้ด |
|---|---|
| AI-first: ข้อความเจ้าของร้านเป็น "ความรู้" ไม่ใช่สคริปต์ยิงตรง | [`buildOwnerPreparedData()`](server.ts:361) + กฎข้อ 21 ใน prompt ([server.ts:5238](server.ts:5238)) |
| ราคามาจาก DB เท่านั้น ลำดับ โปร → display → base | [`resolveProductPricing()`](server.ts:396) + กฎ 6.1 ([server.ts:5282](server.ts:5282)) |
| มีตัวตัดวลี "ราคาเริ่มต้น" ก่อนส่งทุกข้อความ | [`stripStartingPricePhrasing()`](server.ts:948) เรียกที่ [server.ts:364](server.ts:364), [server.ts:5647](server.ts:5647), [server.ts:5980](server.ts:5980) |
| Banned Product Guard 3 ชั้น + สคริปต์ล้างข้อมูล | คำแบน default ([server.ts:598](server.ts:598)), จุดส่งจริง 3 จุด ([server.ts:3483](server.ts:3483), [server.ts:3651](server.ts:3651), [server.ts:3764](server.ts:3764)), ตัดก่อนส่ง ([server.ts:5652](server.ts:5652)), endpoint purge ([server.ts:2303](server.ts:2303)), สคริปต์ [`scripts/purge-banned-product.mjs`](scripts/purge-banned-product.mjs:25) |
| คีย์เวิร์ดทนพิมพ์ผิด (fuzzy) | [server.ts:5693-5702](server.ts:5693) |
| กันตอบซ้ำ + regenerate อุณหภูมิไต่ขึ้น | [server.ts:5613-5644](server.ts:5613) |
| ออเดอร์ตรวจแบบ deterministic + ส่งสรุปยอด | [server.ts:5930-5952](server.ts:5930) |
| ลูกค้าคอมเมนต์ → AI เรียบเรียงข้อความพรีเซนเอง + ส่งเฉพาะรูปที่ตั้งไว้ | [server.ts:4952-4982](server.ts:4952) |

### 2.2 ข้อความที่ "เก่ากว่าโค้ดจริงแล้ว" ⚠️ (handoff เขียนก่อน commit ชุดสุดท้าย)

| คำอ้างใน handoff | ความจริงในโค้ดปัจจุบัน |
|---|---|
| "สเต็ปเหลือ 1 กรณี = ทักแรก/คีย์เวิร์ดแรก + cooldown 24 ชม." | **เลยขั้นนั้นไปแล้ว**: ไม่มีจุดใดเรียก [`sendConfiguredSequenceStep()`](server.ts:3717) อีก (ฟังก์ชันตาย = dead code) และ [`sequencePresentationLog`](server.ts:930) ประกาศไว้แต่ไม่มีใครใช้ → บนเส้นทางแชท **AI เป็นผู้ส่งข้อความแต่เพียงผู้เดียว 100%** สเต็ปเป็น "ข้อมูล" ให้ AI เท่านั้น |
| "HEAD = 11aba92, งาน guard ยังไม่ commit" | เวิร์กสเปซนี้มี commit ถัดมาอีก 5 ตัว (ee92948 → 6c7128a → cbddc4a6 → 8fc314ff → 6ed18e43 → **56798c7 "AI-first brain"**) งาน guard + AI-first commit ครบแล้ว |
| "ต้องรัน purge บน Production เท่านั้น" | ถูกสำหรับ Production แต่ **เครื่องนี้ (local) ก็ยังไม่ได้รัน** — ดูหลักฐานข้อ 3 ด้านล่าง |

---

## 3. 🔴 Root Cause: ทำไมภาพหน้าจอยังเห็น "กล่องตัดยา พกพาง่าย 💊 / ฿990"

ภาพหน้าจอที่ผู้ใช้แนบมา แสดงการ์ดเพจ "เพจหนังมันดูทั้งคืน" (ID 1197031850168519)
ที่มีสินค้า = "กล่องตัดยา พกพาง่าย 💊 ใช้งานสะดวก" ราคาขาย ฿990

การวินิจฉัยเป็นลำดับชั้น:

1. **ไม่ใช่โค้ด** — ค้นทั้งโปรเจกต์แล้ว ไม่มีข้อความ "กล่องตัดยา" หรือเลข 990 ในฐานะค่า hard-code
   ที่ส่งถึงลูกค้า (มีแค่ในรายการคำแบนและคอมเมนต์อธิบาย)
2. **ไม่ใช่ prompt โดยตรง** — prompt ห้ามพูดคำเหล่านี้ และมี guard ตัดก่อนส่ง
3. **ต้นตอจริง = แถวข้อมูลในฐานข้อมูล** (`pages.product` / ตาราง `products`) ของเพจนี้
   ยังเก็บสินค้าเก่าที่ถูกยกเลิกไว้ (ชื่อสินค้า + `display_price = 990`)
   - UI (การ์ดเพจ) อ่านค่านี้มาแสดงตรง ๆ → จึงเห็น ฿990 ในภาพ (**UI ไม่มี guard**)
   - บรรทัด "ข้อมูลสินค้าหลัก" ใน prompt ([server.ts:5244-5248](server.ts:5244)) ก็อ่านชื่อ+ราคาจากแถวเดียวกัน
     → AI จึงเห็นเลข 990 เป็น "ราคาจริงของเพจ" ทุกครั้ง (คำแบนจับวลี "โปรอยู่ 990" ได้
     แต่จับเลข "990" เดี่ยว ๆ ไม่ได้ เพราะจะบล็อกราคาจริงของลูกค้าอื่น)
4. **สรุป**: สิ่งที่ต้องล้างคือ "ข้อมูล" ไม่ใช่ "โค้ด" — สคริปต์
   [`scripts/purge-banned-product.mjs`](scripts/purge-banned-product.mjs:25) ออกแบบมาเพื่อกรณีนี้พอดี
   (ตรวจพบคำว่า "กล่องตัดยา" ใน blob สินค้า → รีเซ็ต product/sequence/steps ของเพจนั้นเป็นค่าว่าง)
   หลังล้าง เพจนี้จะเข้า_flow "เพจยังไม่มีข้อมูลสินค้า → แจ้งแอดมินกรอกใหม่" ([server.ts:5033-5057](server.ts:5033)) ซึ่งถูกต้องตามปรัชญาใหม่

> หมายเหตุ: ไฟล์ [`diagd.json`](diagd.json:1) และ [`diagd2.json`](diagd2.json:1) ในเวิร์กสเปซคือ dump
> วินิจฉัยที่มี blob เพจเก่า (รวม token ที่ mask แล้ว) ติดมาด้วย — ควรลบหลังจบงาน (ต้องยืนยันก่อน)

---

## 4. สถาปัตยกรรมเป้าหมาย (Target Architecture)

```
ลูกค้าทัก/คอมเมนต์
        │
        ▼
 Webhook / Polling ──► เส้นทางเดียว: handleWebhookPost
        │
        ▼
 ┌────────────────────────────────────────────┐
 │ ชั้นความจำ: ประวัติแชท + ลูกค้าเก่า/ใหม่     │
 │ ชั้นความรู้: ข้อมูลสินค้า/โปร/ค่าส่ง จาก DB    │  ← แหล่งเดียว: resolveProductPricing()
 │            + ความรู้เจ้าของร้าน (กรองคำแบน)  │  ← buildOwnerPreparedData()
 └────────────────────────────────────────────┘
        │
        ▼
 🧠 AI (Gemini) = สมองหนึ่งเดียวของ "ข้อความ"
    กฎ 1-21: ตอบเหมือนแอดมินจริง, ห้ามเดา, จำข้อมูลลูกค้า, กันตอบซ้ำ
        │
        ▼
 ┌──────────── ประตูขาออก (Outbound Gates) ────────────┐
 │ 1) ตัดวลี "ราคาเริ่มต้น/เริ่มต้นที่" (stripStartingPricePhrasing) │
 │ 2) Banned Product Guard (scrub/บล็อก)                        │
 │ 3) ส่งจริง Messenger/Comment + บันทึกประวัติเฉพาะของที่ส่งพ้น  │
 └──────────────────────────────────────────────────────┘
        │
        ▼
 ตรวจออเดอร์แบบ deterministic → สร้าง ORD-xxx / ขอข้อมูลเพิ่มเอง
```

หลักการคงที่ 3 ข้อ (ห้ามถอยหลัง):
1. **ข้อความที่ลูกค้าได้รับต้องผ่าน AI เท่านั้น** — ไม่มีเส้นทางยิงสคริปต์ตรงเหลืออยู่
2. **ตัวเลขราคาทุกตัวมาจาก DB** — AI ห้ามพิมพ์เลขเอง
3. **ข้อมูลเก่าที่ปนเปื้อนต้องถูกล้างที่ชั้นข้อมูล** — ไม่ใช่แก้ที่ชั้นข้อความอย่างเดียว

---

## 5. แผนงาน (Work Items) — รอยืนยันก่อนลงมือ

| # | งาน | ไฟล์/ตำแหน่ง | เหตุผล |
|---|---|---|---|
| W1 | รัน purge ข้อมูลเก่า (local ก่อน, Production ทีหลัง) ด้วย `--dry-run` แล้วค่อยรันจริง | [`scripts/purge-banned-product.mjs`](scripts/purge-banned-product.mjs:25) | กำจัดต้นตอ ฿990/กล่องตัดยา ที่ชั้นข้อมูล |
| W2 | ลบ dead code ชุดส่งสเต็ป: `sendConfiguredSequenceStep`, `resolveConfiguredStepImage` (ถ้าไม่ใช้ที่อื่น), `sequencePresentationLog`, `sentSequenceSteps`+`markSequenceStepSent`, บล็อก synthesize สเต็ปที่เหลือแค่ log | [server.ts:3705-3757](server.ts:3705), [server.ts:900-931](server.ts:900), [server.ts:5729-5766](server.ts:5729), [server.ts:6014](server.ts:6014) | ทำให้ "AI เป็นผู้ส่งหนึ่งเดียว" เป็นจริงเชิงโครงสร้าง ไม่ใช่แค่เชิงพฤติกรรม + ลดจุดที่สคริปต์ดิบอาจหลุดส่งตรงได้ในอนาคต |
| W3 | ทำให้ "ราคาเริ่มต้น" ตายสนิท: คง gate ขาออกเดียวไว้เป็นตาข่ายนิรภัย + เพิ่มประโยคใน prompt หลักว่า "ระบบนี้ไม่มีแนวคิดราคาเริ่มต้น ห้ามใช้คำ/ความหมายนี้ ให้报价เป็นรายแพ็กเท่านั้น" | [server.ts:948](server.ts:948), [server.ts:486](server.ts:486), [server.ts:5282](server.ts:5282) | ผู้ใช้สั่งเอาออก "เลย" — เอาออกทุกจุดที่มองเห็นได้ เหลือเพียงตัวกรองกันพลาดชั้นสุดท้ายที่ลูกค้าไม่มีวันเห็น |
| W4 | แก้ UI ให้พูดตรงความจริง: ปุ่ม toggle "Sales Sequence Auto-Trigger" ไม่มีผลต่อพฤติกรรมแล้ว (เหลือแค่ log) → เปลี่ยนคำอธิบายหรือถอดออก, ปรับข้อความแท็บลำดับการขายเป็น "ข้อมูลความรู้สำหรับ AI" | [`src/components/PageSettingsModal.tsx`](src/components/PageSettingsModal.tsx:2779) | กันเจ้าของร้านเข้าใจผิดว่าเปิด/ปิดแล้วมีผล |
| W5 | ลบไฟล์ dump วินิจฉัย `diagd.json`, `diagd2.json` | root | มี blob เพจ+token ติดมา ไม่ควรมีในโปรเจกต์ |
| W6 | ตรวจสอบหลังแก้: `npm run lint`, `npm run build`, ทดสอบผ่าน Live Simulator (เคส: ถามราคา / พิมพ์ "สนใจ" / ลูกค้าเก่า / เพจที่ถูกล้างข้อมูล) | [`src/components/LiveSimulatorTab.tsx`](src/components/LiveSimulatorTab.tsx:433) | ยืนยันพฤติกรรมจริงก่อน deploy |

### สิ่งที่จะ "ไม่แตะ" (ปกป้องของเดิม)
- โครงสร้าง prompt กฎ 1-21, ระบบออเดอร์, Banned Guard, SSE, auth, DB layer — ทำงานถูกต้องอยู่แล้ว
- ฟิลด์ราคาใน DB (`price_1/2/3`, `display_price`, `base_price`) ยังจำเป็นต่อลำดับความสำคัญกฎ 6.1 — ลบฟิลด์ไม่ได้ ลบได้แค่ "คำว่าราคาเริ่มต้น"

---

## 6. ข้อดี-ข้อเสียของการเปลี่ยนครั้งนี้

| มิติ | ข้อดี | ข้อเสี่ยง/วิธีกัน |
|---|---|---|
| ลบ dead code สเต็ป | โค้ดสั้นลง ~100 บรรทัด, ไม่มีเส้นทางสคริปต์ดิบหลุดส่ง, ดูแลง่าย | ต้องตรวจว่า `resolveConfiguredStepImage` ยังถูกใช้ที่ comment-path/fallback ([server.ts:4973](server.ts:4973), [server.ts:5998](server.ts:5998)) → ลบเฉพาะฟังก์ชันส่งสเต็ป ไม่ลบตัวแก้รูป |
| purge ข้อมูล | จบ root cause ของ ฿990 ทั้ง UI/prompt/ประวัติ | เพจจะกลายเป็น "ไม่มีข้อมูลสินค้า" ชั่วคราว → เจ้าของต้องกรอกใหม่ (เป็นพฤติกรรมที่ตั้งใจ) |
| คง gate ตัดคำ "ราคาเริ่มต้น" | ตาข่ายกัน AI รุ่นเพี้ยนพิมพ์คำนี้เอง | ไม่มีผลข้างเคียง เพราะทำงานขาออกเท่านั้น |
| แก้ UI toggle | เจ้าของร้านไม่สับสน | ต้องไม่ลบฟิลด์ `sales_sequence_auto_trigger` ออกจาก schema (เก็บไว้กัน migrate พัง) |

---

## 7. สรุปคำตอบให้ผู้ใช้ (ภาษาคน)

- ระบบการตอบ **เป็น AI สมองหนึ่งเดียวไปแล้วในโค้ด** (สเต็ปไม่ถูกยิงตรงอีกต่อไป) — handoff ข้อนี้ "จริงแต่เก่า" ของจริงเลยไปกว่านั้นอีก
- สิ่งที่ผู้ใช้เห็นเป็นปัญหา (฿990 / กล่องตัดยา) **ไม่ใช่ระบบตอบ แต่เป็นข้อมูลเก่าค้างในฐานข้อมูล** ซึ่งล้างได้ด้วยสคริปต์ที่มีอยู่แล้ว
- คำว่า "ราคาเริ่มต้น" ไม่มีเหลือใน UI/prompt แล้ว เหลือเพียงตัวกรองกันพลาดขาออก ซึ่งแนะนำคงไว้เป็นตาข่ายนิรภัย 1 ชั้น
- งานที่เหลือจริง = ล้างข้อมูล + เก็บซากโค้ดสเต็ป + แก้ป้าย UI ให้พูดตรง + ทดสอบ end-to-end

---

## 8. ✅ ผลการลงมือทำจริง (อัปเดต 2026-09-16, โหมด Code)

ผู้ใช้ยืนยัน: "ทำครบทุกข้อ" — ผลการดำเนินการตาม W1–W6:

| # | งาน | ผล |
|---|---|---|
| W1 | purge ข้อมูล local | ✅ ต่อ [`scripts/purge-banned-product.mjs`](scripts/purge-banned-product.mjs:1) ให้รองรับ PGlite local (เดิมบังคับ DATABASE_URL) → รัน `--dry-run` แล้วรันจริง: **0 hits** = ฐาน local สะอาด → ยืนยันว่า ฿990/กล่องตัดยา อยู่ใน **Production เท่านั้น** (ต้องรัน purge บน Render เอง) |
| W2 | ลบ dead code สเต็ป | ✅ ลบ `sendConfiguredSequenceStep`, `sentSequenceSteps`/`markSequenceStepSent`/`SentStepRecord` + cleanup interval, `sequencePresentationLog`/`SEQUENCE_PRESENTATION_COOLDOWN_MS`, บล็อก synthesize สเต็ป (แทนด้วยนับ `ownerPreparedStepCount` เพื่อ log อย่างเดียว) — คง `resolveConfiguredStepImage()` ไว้ (ยังใช้ใน comment-path/fallback/applyImageSpread) |
| W3 | "ราคาเริ่มต้น" ตายสนิท | ✅ เพิ่มกฎ prompt ข้อ 6.2 (หลัง 6.1): ห้ามใช้คำ/ความหมาย "ราคาเริ่มต้น, เริ่มต้นที่, เริ่มต้น ฿, ตั้งแต่ ~ ขึ้นไป" เด็ดขาด ให้บอกราคาเป็นรายแพ็กเท่านั้น + คง [`stripStartingPricePhrasing()`](server.ts:914) เป็นตาข่ายนิรภัยขาออกตามผู้ใช้เลือก |
| W4 | แก้ป้าย UI | ✅ [`PageSettingsModal.tsx`](src/components/PageSettingsModal.tsx:1): แท็บ "2. แพตเทิร์น 6 สเต็ป + รูปภาพ" → "2. ข้อมูลร้านให้ AI (ข้อความ+รูป)", ปุ่ม → "+ เพิ่มชุดข้อมูลให้ AI", ถอด toggle "Sales Sequence Auto-Trigger" → การ์ดแจ้ง "🧠 AI เป็นผู้ตอบหนึ่งเดียว (AI-first)" (ฟิลด์ยังอยู่ใน DB แต่ไม่มีผลแล้ว) |
| W5 | ลบไฟล์ dump | ✅ ลบ `diagd.json`, `diagd2.json` แล้ว |
| W6 | ทดสอบ | ✅ `npm run lint` ผ่าน (exit 0) ✅ `npm run build` ผ่าน (exit 0) ✅ `npm run selftest`: **42 ผ่าน / 3 เตือน / 1 พัง** — 3 เตือน (webhook E2E, Vision, Audio fallback เป็น template) เกิดจาก Gemini API key ในเครื่อง local ไม่ valid; 1 พัง (follow-up) เกิดจาก `PAGE_ACCESS_TOKEN_NOT_CONFIGURED` ของเพจทดสอบ — **ทั้งหมดเป็นปัญหาสิ่งแวดล้อม ไม่ใช่ regression จากการแก้โค้ด** (หมายเหตุ: ตัว selftest crash ตอนจบด้วย libuv assertion บน Windows หลังพิมพ์สรุปแล้ว — เป็นบั๊กแวดล้อมที่พบอยู่เดิม) |

### ขั้นตอนที่เหลือสำหรับผู้ใช้ (Production)
1. commit + push การเปลี่ยนแปลงทั้งหมดขึ้น `origin/main` (Render จะ auto-deploy)
2. บน Render: ตั้ง `DATABASE_URL` แล้วรัน `node scripts/purge-banned-product.mjs --dry-run` ตรวจผลก่อน
3. ถ้า dry-run เจอแถว ฿990/กล่องตัดยา → รันจริง (ไม่ใส่ `--dry-run`) เพื่อล้างข้อมูล Production
4. ทดสอบซ้ำผ่าน Live Simulator / เพจจริงว่าการ์ดเพจไม่แสดง ฿990 อีก
