# 🚀 Instant Sales Engine — สถาปัตยกรรมตอบเร็วแบบนักปิดการขาย

> เอกสารออกแบบ (Architecture Design) — ยังไม่มีการแก้โค้ด รอการยืนยันแผนก่อน
> วันที่: 2026-09-17 | โหมด: Architect

---

## 1. ปัญหาที่ผู้ใช้รายงาน

1. ลูกค้าทัก "สนใจ" → ระบบตอบช้ามาก **เกือบ 30 วินาที**
2. คำตอบที่ได้คือ **"กำลังตรวจสอบรายละเอียดสินค้าให้ค่ะ รอสักครู่นะคะ 🙏"** — ข้อความถ่วงเวลาที่ไม่ขายอะไรเลย
3. ต้องการ: ตอบเร็วที่สุด + ฉลาดแบบมนุษย์ + พรีเซน **ข้อความ + รูปภาพ + ข้อมูลที่มี** เหมือนนักปิดการขายมือทอง + **ใช้ได้ทุกเพจ**

## 2. รากเหง้า (Root Cause Analysis) — อ้างอิงโค้ดจริง

### 2.1 หนึ่งคำตอบเรียก AI ต่อกันสูงสุด 4 ครั้ง (Serial AI Calls)

| # | จุดเรียก | ตำแหน่ง | งบเวลา |
|---|---------|---------|--------|
| 1 | `generateAiJson(promptContext, { maxOutputTokens: 2048 })` — AI หลัก | server.ts:5366 | primary 20s + fast retry 10s **ต่อโมเดล** ไล่ได้ถึง 6 โมเดล ภายใน deadline 35s |
| 2 | `generateAiClosingMessage()` — AI เขียนปิดการขายซ้ำอีกรอบ | server.ts:5870 | +8-15s |
| 3 | `requestOrderInfoViaAi()` — AI ขอข้อมูลสั่งซื้อใหม่ | server.ts:5913 | +8-15s |
| 4 | `generateAiFallbackReply()` — AI สำรองเมื่อหลักพัง | server.ts:5999 | +30s แล้วจบที่ข้อความถ่วงเวลา (server.ts:6006) |

### 2.2 ค่าคงที่เวลา "เผื่อไว้เยอะเกิน" (server.ts:214-218)

```
AI_TIMEOUT_MS    = 20000  (20 วิต่อโมเดลหลัก)
AI_FAST_RETRY_MS = 10000  (10 วิต่อโมเดลด่วน)
AI_DEADLINE_MS   = 35000  (งบรวม 35 วิ — ตรงกับที่ผู้ใช้เจอ "เกือบ 30 วิ")
maxOutputTokens  = 2048   (thinking model ใช้ token ไปกับ "การคิด" → ช้า)
```

### 2.3 ข้อความถ่วงเวลาคือ "ทางตัน" ที่ไม่ใช้ข้อมูลที่มีอยู่

เมื่อ AI พังทั้งกระดาน ระบบตอบ "กำลังตรวจสอบรายละเอียดสินค้าให้ค่ะ รอสักครู่นะคะ 🙏"
ทั้งที่ **DB มีครบทุกอย่าง**: ชื่อสินค้า ราคาจริง (resolveProductPricing) โปรโมชั่น ของแถม ค่าส่ง รูป 5 ช่อง — ข้อมูลชุดเดียวกับที่ป้อนให้ AI นั่นเอง แต่ไม่เคยถูกเอามาประกอบเป็นคำตอบขายตรง ๆ

มีข้อความถ่วงเวลาแบบนี้ 4 จุด: server.ts:5447, 6006, 6018, 8226

## 3. สถาปัตยกรรมเป้าหมาย: Two-Tier Response

```
ลูกค้าทัก "สนใจ"
      │
      ▼
┌─────────────────────────────────────────────────────────┐
│ Tier 1: INSTANT ENGINE (ท้องถิ่น, 0ms, ไม่ต้องรอ AI)     │
│  • typing_on → ส่ง "ข้อความทัก + รูป main" ทันที (<2 วิ)  │
│  • ประกอบจากข้อมูลเพจจริง: resolveProductPricing + imgMap │
│  • pushHistory → AI เห็นว่าแอดมินทักแล้ว (ไม่ทักซ้ำ)      │
└─────────────────────────────────────────────────────────┘
      │  (ทำงานขนาน)
      ▼
┌─────────────────────────────────────────────────────────┐
│ Tier 2: AI BRAIN (งบเวลาเข้มงวด: deadline 12 วิ)          │
│  • ชนะทันเวลา → ส่งคำตอบ AI (ฉลาด รู้บริบท) + รูปตามเจตนา │
│  • แพ้/พัง    → Tier 1 ส่งชุดพรีเซนเต็มต่อทันที            │
│                 (จุดขาย → ราคา/โปร → ของแถม/ส่ง → ปิดการขาย)│
└─────────────────────────────────────────────────────────┘
      │
      ▼
  ข้อความปิดการขาย (local template 0ms — เลิกเรียก AI ซ้ำ)
```

### 3.1 ฟังก์ชันใหม่ (module scope ใน server.ts — ใช้ได้ทุกเพจ เพราะอ่านจาก page data ล้วน)

| ฟังก์ชัน | หน้าที่ | Input |
|---------|--------|-------|
| `buildInstantAck(page, intentHint)` | ข้อความแรกตอบไว (<120 ตัวอักษร) + รูป main — มี 3 สำนวนหมุนเวียนกัน (anti-repeat) | page, intent |
| `buildInstantSalesReply(page, matchedProduct, intentHint, imgMap)` | ชุดพรีเซนเต็ม 3-5 ข้อความ + รูปเรียงตามเจตนา — ใช้ราคาจริงจาก `resolveProductPricing()` เท่านั้น ไม่มีข้อมูลส่วนไหน → ข้ามข้อความนั้น (ไม่เดา) | page data |
| `buildLocalClosingAsk(page, matchedProduct)` | ข้อความปิดการขาย 2 ข้อความตามแพทเทิร์นที่ผู้ใช้ให้มา: "สนใจรับเป็นชุดไหนดีคะ 😊" + "สนใจสั่งซื้อ แจ้งชื่อ ที่อยู่ เบอร์โทรผู้รับให้หน่อยนะคะ เดี๋ยวแอดมินสรุปยอดให้ค่ะ" (ปรับชื่อสินค้า/แพ็กจริง) | page data |
| `buildLocalOrderInfoRequest(problems)` | ขอข้อมูลสั่งซื้อใหม่จาก `ORDER_PROBLEM_FIX_HINT` ที่มีอยู่แล้ว — ไม่ต้องเรียก AI | problems[] |

ทุกข้อความผ่าน `sanitizeCustomerFacingText()` (COD → เก็บเงินปลายทาง) + `stripStartingPricePhrasing()` + `scrubBannedProductContent()` + `splitLongOutgoingText()` เหมือนขาออก AI ทุกประการ

### 3.2 ปรับค่าคงที่เวลา (คง env override ได้เหมือนเดิม)

```
AI_TIMEOUT_MS    : 20000 → 8000   (8 วิต่อโมเดลหลัก)
AI_FAST_RETRY_MS : 10000 → 5000   (5 วิโมเดลด่วน)
AI_DEADLINE_MS   : 35000 → 12000  (งบรวม 12 วิ — พอเพราะ Tier 1 ตอบไปแล้ว ลูกค้าไม่รอเก้อ)
maxOutputTokens  : 2048  → 900    (คำตอบคือข้อความสั้น 3-5 ข้อความ 900 เหลือเฟือ → คิดเร็วขึ้น)
```

### 3.3 ตัดการเรียก AI ที่ซ้ำซ้อนออก (ลด latency + ลดค่า token)

| จุด | เดิม | ใหม่ |
|-----|------|------|
| ข้อความปิดการขาย (5870) | เรียก AI ซ้ำ (+8-15 วิ) | `buildLocalClosingAsk()` — 0ms ได้แพทเทิร์นที่ผู้ใช้ต้องการเป๊ะ |
| ขอข้อมูลสั่งซื้อใหม่ (5913) | เรียก AI ซ้ำ | `buildLocalOrderInfoRequest()` — 0ms |
| AI พัง → fallback (5999) | เรียก AI ซ้ำ → พังอีก → "รอสักครู่" | `buildInstantSalesReply()` — พรีเซนขายเต็มชุดทันที ไม่มีถ่วงเวลา |
| replyText ว่าง (5441-5451) | เรียก AI ซ้ำ → "รอสักครู่" | `buildInstantSalesReply()` |
| Follow-up engine (8226) | "กำลังตรวจสอบ...รอสักครู่" | `buildInstantSalesReply()` (follow-up ไม่เร่ง latency แต่ห้ามถ่วงเวลาเหมือนกัน) |

### 3.4 Prompt rule ใหม่ (กัน AI ทักทายซ้ำ)

เพิ่มกฎใน promptContext: *"แอดมินได้ส่งข้อความทักทาย/รูปแรกไปแล้ว (ดูประวัติล่าสุด) — ห้ามทักทายซ้ำ ห้ามส่งรูป main ซ้ำ ให้เข้าเนื้อหา/ปิดการขายต่อทันที"*

### 3.5 ลำดับการทำงานใหม่ใน handleWebhookPost

```
1. typing_on                                    (มีอยู่แล้ว)
2. ⚡ ส่ง buildInstantAck() + รูป main ทันที      (ใหม่ — <2 วิ)
   → pushHistory + recordSimulatedSend
3. เรียก AI หลัก (deadline 12 วิ, output 900)
4. AI สำเร็จ → ส่ง outgoing (ข้อความ+รูปตามเจตนา) → closing local template
   AI พัง    → ส่ง buildInstantSalesReply() เต็มชุด → closing local template
5. typing_off                                    (มีอยู่แล้ว)
```

## 4. ผลลัพธ์ที่คาดหวัง (วัดได้)

| สถานการณ์ | เดิม | ใหม่ |
|-----------|------|------|
| ข้อความแรกถึงลูกค้า | 8-30 วิ | **< 2 วิ เสมอ** |
| AI ปกติ | 8-15 วิ + closing AI อีก 8-15 วิ | ทักทันที 2 วิ + คำตอบ AI 3-8 วิ + closing 0ms |
| AI พัง/ช้า | 30 วิ + "รอสักครู่นะคะ 🙏" | ทักทันที 2 วิ + พรีเซนเต็มชุดจากข้อมูลจริง จบใน ~6 วิ |
| จำนวน AI call ต่อข้อความ | สูงสุด 4 | **1** |
| ใช้ได้ทุกเพจ | — | ✅ อ่านจาก page.product/promotions/images ล้วน ไม่มี hardcode |

## 5. ข้อดี / ข้อเสีย / ความเสี่ยง

**ข้อดี**
- ลูกค้าไม่มีวันรอเก้อ — ข้อความแรก + รูป มาใน 2 วิ (แก้ปัญหา "ลูกค้าหาย" ตรงจุด)
- เลิกมีข้อความถ่วงเวลา — ทุกกรณีคือ "การขาย" จากข้อมูลจริง
- ลดค่า token ~60-75% (ตัด AI call ซ้ำ 3 จุด)
- แม่นยำเรื่องราคาเท่าเดิม/ดีกว่า — local engine ใช้ `resolveProductPricing()` แหล่งเดียวกับ AI (single source of truth) ไม่มีการเดา

**ข้อเสีย / ความเสี่ยง + วิธีคุม**
- ข้อความ template ซ้ำกันเมื่อลูกค้าทักหลายรอบ → หมุนเวียน 3 สำนวน + ผ่าน `isRepeatedReply()` ไม่ได้ (ส่งตรง) แต่ ack สั้นและต่างกันตาม intent
- AI สำเร็จ → ลูกค้าได้ข้อความเยอะขึ้น 1 ข้อความ (ack + คำตอบ AI) → ack สั้น <120 ตัวอักษร เหมือนแอดมินทักก่อนตอบจริง (พฤติกรรมมนุษย์ปกติ)
- เพจที่ไม่มีข้อมูลสินค้าเลย (ว่างทุกอย่าง) → ack จะทัก + ถามความต้องการแทน ไม่พรีเซนมั่ว (graceful degradation)

## 6. แผนการทำงาน (แบ่งเป็นขั้น ตรวจสอบได้ทีละขั้น)

- [ ] **ขั้น 1**: เพิ่ม 4 ฟังก์ชัน local engine (module scope) + unit test `scripts/_test_instant_reply.mjs` (สกัดฟังก์ชันจริงจาก server.ts มาทดสอบเหมือน `_test_split_sanitize.mjs`)
- [ ] **ขั้น 2**: ปรับค่าคงที่เวลา (214-218) + maxOutputTokens ของ main call
- [ ] **ขั้น 3**: ฝัง Instant Ack ใน webhook (หลัง typing_on) + prompt rule กันทักซ้ำ
- [ ] **ขั้น 4**: เปลี่ยน closing/order-info/fallback ทุกจุดเป็น local engine + ลบข้อความ "รอสักครู่" ทั้ง 4 จุด
- [ ] **ขั้น 5**: lint + build + E2E simulate เคส "สนใจ" (AI key พัง = จำลอง AI ล่ม → ต้องได้พรีเซนเต็มชุด + รูป ใน <6 วิ ไม่มีคำว่ารอ)
- [ ] **ขั้น 6**: สรุปผล + ขั้นตอน deploy (commit/push + Render)

## 7. สิ่งที่ไม่แตะ (คงของเดิมทั้งหมด)

- Pipeline ฝั่ง AI สำเร็จ: buildOutgoing → autoAttachImages → applyImageSpread → PRICE GUARD → anti-repeat → sanitize → split → send loop (ที่แก้ไว้รอบก่อนทั้งหมด)
- ระบบ order/dispatch/LINE/Telegram, follow-up engine (ยกเว้นข้อความถ่วงเวลา), banned product guard
- โครงสร้าง DB / API / หน้า UI
